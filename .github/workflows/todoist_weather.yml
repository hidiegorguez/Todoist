name: Todoist Weather

on:
  schedule:
    - cron: '53 12 * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Runtime diagnostics
        run: |
          echo "[diag] UTC now: $(date -u)"
          echo "[diag] ref: $GITHUB_REF"
          echo "[diag] actor: $GITHUB_ACTOR"
          python --version
          pip --version

      - name: Run TodoistWeather
        env:
          TODOIST_API_TOKEN: ${{ secrets.TODOIST_API_TOKEN }}
          OPEN_WEATHER_API_KEY: ${{ secrets.OPEN_WEATHER_API_KEY }}
        run: |
          python - <<'PY'
          import datetime
          import os
          import traceback
          from todoist_automation.api.todoist_client import TodoistFunctions
          from todoist_automation.automations import weather

          print(f"[TodoistWeather] start UTC={datetime.datetime.utcnow().isoformat()}Z")
          try:
              tf = TodoistFunctions(api_token=os.getenv('TODOIST_API_TOKEN'))
              result = weather.run_weather(tf)
              print(f"[TodoistWeather] result: {result}")
              print("[TodoistWeather] completed")
          except Exception as exc:
              print(f"[TodoistWeather] failed: {exc}")
              traceback.print_exc()
              raise
          PY
